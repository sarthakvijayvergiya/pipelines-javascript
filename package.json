{
  "name": "HelloWorld",
  "private": true,
  "version": "0.0.0",
  "description": "Hello World",
  "main": "server.js",
  "author": {
    "name": "",
    "email": ""
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "express": "^4.13.3",
    "mocha": "^3.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "gulp": "^3.9.0",
    "gulp-istanbul": "^1.1.3",
    "gulp-mocha": "^2.2.0",
    "mocha-junit-reporter": "^1.12.0",
    "nyc": "^12.0.2"
  },
  "scripts": {
    "build": "babel --out-dir /build ./server.js",
    "build-dev": "nyc --reporter=cobertura --reporter=html ./node_modules/.bin/mocha tests/**/*.js --reporter mocha-junit-reporter --reporter-options mochaFile=./TEST-RESULTS.xml"
  }
}
